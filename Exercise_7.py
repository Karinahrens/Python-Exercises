# Exercise 7 (Stretch)
# Write a decorator function @slow_down that takes another function as argument and slows
# the execution of the function down and only executes the function after 10 seconds. The
# function that takes the decorator should print out "Hello!". Test the code using examples.